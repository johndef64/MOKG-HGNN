# Smoke test for the baseline comparison ablation.
# 2 architectures × 2 seeds = 4 runs.
experiments:
- name: smoke_gcn_seed_2025
  overrides: { project: { seed: 2025 }, model: { name: gcn_tf }, train: { num_epochs: 10 } }
- name: smoke_gcn_seed_2026
  overrides: { project: { seed: 2026 }, model: { name: gcn_tf }, train: { num_epochs: 10 } }
- name: smoke_gat_seed_2025
  overrides: { project: { seed: 2025 }, model: { name: gat_tf }, train: { num_epochs: 10 } }
- name: smoke_gat_seed_2026
  overrides: { project: { seed: 2026 }, model: { name: gat_tf }, train: { num_epochs: 10 } }
